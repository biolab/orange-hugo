{
    "uri": "{{ .Permalink }}",
	"title": "{{ .Title}}",
	"content": {{ .Plain | jsonify }},
	"images": [{{ range $tindex, $image := .Params.images }}{{ if $tindex }}, {{ end }}"{{ $image| htmlEscape }}"{{ end }}],
	"download": "{{ .Params.download}}",
	"blog" : "{{ .Params.blog}}",
	"video" : "{{ .Params.video}}",
	"workflows": [{{ range $tindex, $workflows := .Params.workflows }}{{ if $tindex }}, {{ end }}"{{ $workflows| htmlEscape }}"{{ end }}]
}
